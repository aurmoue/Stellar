export class Level
{
    constructor(chartName, composer, chartPath)
    {
        this.chartName = chartName;
        this.composer = composer;
        this.chartPath = chartPath;

        this.score = "0000000";
    }
}